#!/usr/bin/env bash

source ..../friends_venv/bin/activate         # add path-to-your-virtual-environment in placeholder; activates it
cd ..../friends_episode_selector/             # add path to the repository/directory to navigate there
python3 episode_selector.py                   # run the program